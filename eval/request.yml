# Committing a change to this file starts an evaluation round
# (.github/workflows/eval.yml): every topic in config/eval-topics.yml (or the
# listed numbers) is generated in parallel with --dry-run (no PRs), and the
# report is committed to the `state` branch under evals/round-<round>/.
round: 1
topics: all
note: Baseline — Phase 2 prompts as first written, no tuning yet.
